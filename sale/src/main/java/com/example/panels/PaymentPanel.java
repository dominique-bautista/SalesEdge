package com.example.panels;

import javax.swing.*;
import java.awt.*;

public class PaymentPanel extends JPanel {
    public PaymentPanel() {
        setBackground(Color.PINK);
        add(new JLabel("Payment Section", SwingConstants.CENTER));
    }
}
