package com.example.panels;

import javax.swing.*;
import java.awt.*;

public class DataPanel extends JPanel {
    public DataPanel() {
        setBackground(Color.LIGHT_GRAY);
        add(new JLabel("Data Section", SwingConstants.CENTER));
    }
}
