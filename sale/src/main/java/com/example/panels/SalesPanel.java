package com.example.panels;

import javax.swing.*;
import java.awt.*;

public class SalesPanel extends JPanel {
    public SalesPanel() {
        setBackground(Color.GREEN);
        add(new JLabel("Sales Section", SwingConstants.CENTER));
    }
}
